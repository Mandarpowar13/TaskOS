import { useQuery } from '@tanstack/react-query'
import { Area, AreaChart, Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { BarChart3, CircleAlert, LoaderCircle } from 'lucide-react'
import { getAnalytics } from '../../services/analyticsService'

export default function AnalyticsPage() { const { data, isLoading, isError } = useQuery({ queryKey: ['analytics'], queryFn: getAnalytics }); if (isLoading) return <div className="grid min-h-96 place-items-center text-slate-400"><LoaderCircle className="animate-spin" /></div>; if (isError) return <div className="card grid min-h-96 place-items-center text-center"><div><CircleAlert className="mx-auto mb-3 text-rose-500" /><p className="font-semibold dark:text-white">Analytics are unavailable</p></div></div>; const metrics = data.metrics; const priority = data.byPriority.map((item) => ({ name: item._id, value: item.count })); return <div className="mx-auto max-w-[1180px]"><section className="mb-7"><p className="mb-2 text-sm font-medium text-blue-600 dark:text-blue-400">Work intelligence</p><h1 className="text-2xl font-bold tracking-tight text-slate-900 dark:text-white sm:text-3xl">Analytics</h1><p className="mt-2 text-sm text-slate-500">Understand the momentum behind your work.</p></section><section className="mb-6 grid gap-4 sm:grid-cols-4">{[['Completion rate', `${metrics.completionRate}%`], ['Completed', metrics.completed], ['Pending', metrics.pending], ['Overdue', metrics.overdue]].map(([label, value]) => <div key={label} className="card p-5"><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold text-slate-900 dark:text-white">{value}</p></div>)}</section><section className="grid gap-6 lg:grid-cols-2"><div className="card p-5"><div className="mb-5 flex items-center gap-2"><BarChart3 size={19} className="text-blue-600" /><div><h2 className="font-semibold dark:text-white">Monthly activity</h2><p className="text-xs text-slate-500">Created and completed tasks</p></div></div><div className="h-64"><ResponsiveContainer><AreaChart data={data.monthlyTrend}><defs><linearGradient id="analytics-area" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#2563eb" stopOpacity=".3" /><stop offset="100%" stopColor="#2563eb" stopOpacity="0" /></linearGradient></defs><XAxis dataKey="_id" tickLine={false} axisLine={false} /><YAxis tickLine={false} axisLine={false} /><Tooltip /><Area type="monotone" dataKey="completed" stroke="#2563eb" fill="url(#analytics-area)" strokeWidth={2.5} /></AreaChart></ResponsiveContainer></div></div><div className="card p-5"><h2 className="font-semibold dark:text-white">Work by priority</h2><p className="mt-1 text-xs text-slate-500">Your current workload distribution</p><div className="mt-5 h-64"><ResponsiveContainer><BarChart data={priority}><XAxis dataKey="name" tickLine={false} axisLine={false} /><YAxis allowDecimals={false} tickLine={false} axisLine={false} /><Tooltip /><Bar dataKey="value" fill="#2563eb" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></div></div></section></div> }
