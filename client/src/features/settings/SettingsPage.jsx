import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { CircleAlert, LoaderCircle, Plus, Tag, Trash2 } from 'lucide-react'
import { useState } from 'react'
import { createCategory, deleteCategory, getCategories } from '../../services/categoryService'

export default function SettingsPage() {
  const queryClient = useQueryClient(); const [name, setName] = useState(''); const [color, setColor] = useState('#2563eb'); const [error, setError] = useState('')
  const { data: categories = [], isLoading } = useQuery({ queryKey: ['categories'], queryFn: getCategories })
  const refresh = () => queryClient.invalidateQueries({ queryKey: ['categories'] })
  const createMutation = useMutation({ mutationFn: createCategory, onSuccess: () => { setName(''); refresh() }, onError: (e) => setError(e.response?.data?.message || 'Could not create category.') })
  const deleteMutation = useMutation({ mutationFn: deleteCategory, onSuccess: refresh, onError: (e) => setError(e.response?.data?.message || 'Could not delete category.') })
  function addCategory(event) { event.preventDefault(); if (!name.trim()) return; setError(''); createMutation.mutate({ name: name.trim(), color }) }
  return <div className="mx-auto max-w-4xl"><section className="mb-7"><p className="mb-2 text-sm font-medium text-blue-600 dark:text-blue-400">Workspace preferences</p><h1 className="text-2xl font-bold tracking-tight text-slate-900 dark:text-white sm:text-3xl">Settings</h1><p className="mt-2 text-sm text-slate-500 dark:text-slate-400">Configure the building blocks that organize your work.</p></section><section className="card p-5 sm:p-6"><div className="flex items-start gap-3"><div className="rounded-xl bg-blue-50 p-2.5 text-blue-600 dark:bg-blue-500/10 dark:text-blue-400"><Tag size={20} /></div><div><h2 className="font-semibold text-slate-900 dark:text-white">Categories</h2><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Create custom labels for different kinds of work.</p></div></div><form onSubmit={addCategory} className="mt-6 flex flex-col gap-3 sm:flex-row"><input value={name} onChange={(event) => setName(event.target.value)} maxLength="80" placeholder="e.g. Research" className="h-11 flex-1 rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-blue-400 focus:ring-4 focus:ring-blue-500/10 dark:border-slate-700 dark:bg-slate-800 dark:text-white" /><input aria-label="Category color" type="color" value={color} onChange={(event) => setColor(event.target.value)} className="h-11 w-full rounded-xl border border-slate-200 bg-white p-1 sm:w-14 dark:border-slate-700 dark:bg-slate-800" /><button disabled={createMutation.isPending} className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-semibold text-white hover:bg-blue-700 disabled:opacity-70"><Plus size={17} />Add category</button></form>{error && <p className="mt-4 flex items-center gap-2 text-sm text-rose-600"><CircleAlert size={16} />{error}</p>}<div className="mt-6 divide-y divide-slate-100 border-t border-slate-100 dark:divide-slate-800 dark:border-slate-800">{isLoading ? <div className="grid h-24 place-items-center text-slate-400"><LoaderCircle className="animate-spin" size={20} /></div> : categories.map((category) => <div key={category._id} className="flex items-center justify-between py-3"><div className="flex items-center gap-3"><span className="h-3 w-3 rounded-full" style={{ backgroundColor: category.color }} /><span className="text-sm font-medium text-slate-800 dark:text-slate-200">{category.name}</span>{category.isDefault && <span className="rounded-md bg-slate-100 px-2 py-0.5 text-[10px] font-bold uppercase tracking-wide text-slate-500 dark:bg-slate-800">Default</span>}</div>{!category.isDefault && <button disabled={deleteMutation.isPending} onClick={() => { if (window.confirm(`Delete “${category.name}”?`)) deleteMutation.mutate(category._id) }} aria-label={`Delete ${category.name}`} className="rounded-lg p-2 text-slate-400 hover:bg-rose-50 hover:text-rose-600 dark:hover:bg-rose-500/10"><Trash2 size={16} /></button>}</div>)}{!isLoading && categories.length === 0 && <p className="py-6 text-center text-sm text-slate-500">No categories yet. Create one to organize your tasks.</p>}</div></section></div>
}
